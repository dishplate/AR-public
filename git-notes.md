## Git cheatsheet

git-init - Create an empty Git repository or reinitialize an existing one
git status - run this often to check on your git or to tell if a folder is monitored by git

.git hidden folder is in every git initialized folder
Remove git from a folder by deleting the .git folder



